{
  "rules": [
    {
      "allowed": {
        "origins": ["http://localhost:3000", "https://demoify.app"],
        "methods": ["PUT", "GET", "HEAD"],
        "headers": ["content-type"]
      },
      "exposeHeaders": ["ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
